import * as React from "react";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";

const GraficaPieChart = ({ title, description, data }) => {
  return (
    <Card className="flex flex-col w-full max-w-[500px]">
      pendiente https://ui.shadcn.com/charts/pie#charts
    </Card>
  );
};

export default GraficaPieChart;
